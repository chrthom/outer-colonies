class AnimationConfig {
    readonly duration = {
        draw: 400,
        move: 500,
        showTacticCard: 600,
        waitBeforeDiscard: 2000,
        buffer: 50
    }
}

export const animationConfig = new AnimationConfig();