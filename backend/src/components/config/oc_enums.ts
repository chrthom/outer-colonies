export enum CardType {
    Hull = 'hull',
    Equipment = 'equipment',
    Colony = 'colony',
    Tactic = 'tactic',
    Orb = 'orb'
}

export enum MesgType {
    state = 'state'
}

export enum TurnPhase {
    Start = 'start',
    Build = 'build',
    Plan = 'plan',
    Fight = 'fight',
    End = 'end'
}

export enum Zone {
    Colony = 'colony',
    Oribital = 'orbital',
    Neutral = 'neutral'
}
